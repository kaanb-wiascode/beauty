import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '@beauty-erp/database';
import { TenantContext } from '../../common/tenant/tenant-context';
const row=(value:any)=>value;
@Injectable()
export class InventoryService {
 constructor(private readonly prisma:PrismaService,private readonly tenantContext:TenantContext){}
 private tenantId(){const id=this.tenantContext.getTenantId();if(!id)throw new BadRequestException('Tenant context is missing');return id}
 private companyId(){const id=this.tenantContext.getCompanyId();if(!id)throw new BadRequestException('Company context is missing');return id}
 private async ensureWarehouses(){const tenantId=this.tenantId(),companyId=this.companyId(),branchId=this.tenantContext.getBranchId();await this.prisma.$executeRawUnsafe(`INSERT INTO inventory_warehouses(tenant_id,company_id,name,type) VALUES($1::text,$2::text,'Ana Depo','MAIN_DEPOT') ON CONFLICT DO NOTHING`,tenantId,companyId);if(branchId){const b=await this.prisma.$queryRawUnsafe<any[]>(`SELECT id,name FROM branches WHERE id=$1::text AND company_id=$2::text AND status='ACTIVE' LIMIT 1`,branchId,companyId);if(!b.length)throw new NotFoundException('Branch not found');await this.prisma.$executeRawUnsafe(`INSERT INTO inventory_warehouses(tenant_id,company_id,branch_id,name,type) VALUES($1::text,$2::text,$3::text,$4,'BRANCH') ON CONFLICT DO NOTHING`,tenantId,companyId,branchId,`${b[0].name} Stok`)}}
 async overview(){await this.ensureWarehouses();const companyId=this.companyId(),branchId=this.tenantContext.getBranchId();const filter=branchId?`w.branch_id='${branchId}'::text`:`w.company_id='${companyId}'::text`;const [m,c,w,p]=await Promise.all([this.prisma.$queryRawUnsafe<any[]>(`SELECT COUNT(DISTINCT p.id)::int AS "totalProducts",COUNT(DISTINCT CASE WHEN s.quantity<=s.minimum_quantity AND s.minimum_quantity>0 THEN p.id END)::int AS "criticalProducts",COALESCE(SUM(s.quantity*s.cost_per_unit),0)::numeric AS "inventoryValue" FROM inventory_products p LEFT JOIN inventory_stock s ON s.product_id=p.id LEFT JOIN inventory_warehouses w ON w.id=s.warehouse_id AND ${filter} WHERE p.company_id=$1::text AND p.status='ACTIVE'`,companyId),this.prisma.$queryRawUnsafe<any[]>(`SELECT p.id,p.name,p.sku,p.unit,w.id AS "warehouseId",w.name AS "warehouseName",w.type AS "warehouseType",s.quantity,s.minimum_quantity AS "minimumQuantity",s.target_quantity AS "targetQuantity" FROM inventory_stock s JOIN inventory_products p ON p.id=s.product_id JOIN inventory_warehouses w ON w.id=s.warehouse_id WHERE p.company_id=$1::text AND p.status='ACTIVE' AND ${filter} AND s.quantity<=s.minimum_quantity AND s.minimum_quantity>0 ORDER BY s.quantity ASC,p.name ASC LIMIT 12`,companyId),this.prisma.$queryRawUnsafe<any[]>(`SELECT id,name,type,branch_id AS "branchId" FROM inventory_warehouses WHERE company_id=$1::text AND status='ACTIVE' ORDER BY type,name`,companyId),this.purchaseRequests()]);return{metrics:row(m[0]??{}),critical:c,warehouses:w,purchaseRequests:p}}
 async products(search?:string){await this.ensureWarehouses();const companyId=this.companyId(),branchId=this.tenantContext.getBranchId(),filter=branchId?`w.branch_id='${branchId}'::text`:`w.company_id='${companyId}'::text`;return this.prisma.$queryRawUnsafe<any[]>(`SELECT p.id,p.name,p.sku,p.brand,p.description,p.unit,p.status,p.track_stock AS "trackStock",p.track_expiry AS "trackExpiry",COALESCE(SUM(CASE WHEN ${filter} THEN s.quantity ELSE 0 END),0) AS quantity,COALESCE(SUM(CASE WHEN ${filter} THEN s.minimum_quantity ELSE 0 END),0) AS "minimumQuantity",COALESCE(SUM(CASE WHEN ${filter} THEN s.target_quantity ELSE 0 END),0) AS "targetQuantity" FROM inventory_products p LEFT JOIN inventory_stock s ON s.product_id=p.id LEFT JOIN inventory_warehouses w ON w.id=s.warehouse_id WHERE p.company_id=$1::text AND ($2::text IS NULL OR p.name ILIKE '%'||$2||'%' OR COALESCE(p.sku,'') ILIKE '%'||$2||'%') GROUP BY p.id ORDER BY p.name`,companyId,search?.trim()||null)}
 async createProduct(input:any){await this.ensureWarehouses();const tenantId=this.tenantId(),companyId=this.companyId(),warehouseId=input.warehouseId??await this.defaultWarehouseId(),units=['UNIT','ML','LITER','GRAM','KG','METER','PAIR','BOX'];if(!units.includes(input.unit??'UNIT'))throw new BadRequestException('Invalid inventory unit');if(!input.name?.trim())throw new BadRequestException('Product name is required');const created=await this.prisma.$queryRawUnsafe<any[]>(`INSERT INTO inventory_products(tenant_id,company_id,name,sku,brand,description,unit) VALUES($1::text,$2::text,$3,$4,$5,$6,$7::"InventoryUnit") RETURNING id,name,sku,brand,description,unit,status`,tenantId,companyId,input.name.trim(),input.sku?.trim()||null,input.brand?.trim()||null,input.description?.trim()||null,input.unit??'UNIT');const product=created[0];await this.prisma.$executeRawUnsafe(`INSERT INTO inventory_stock(product_id,warehouse_id,quantity,minimum_quantity,target_quantity) VALUES($1::text,$2::text,$3,$4,$5) ON CONFLICT(product_id,warehouse_id) DO UPDATE SET minimum_quantity=EXCLUDED.minimum_quantity,target_quantity=EXCLUDED.target_quantity,quantity=inventory_stock.quantity+EXCLUDED.quantity,updated_at=NOW()`,product.id,warehouseId,Number(input.initialQuantity||0),Number(input.minimumQuantity||0),Number(input.targetQuantity||0));if(Number(input.initialQuantity||0)>0)await this.addMovement(product.id,warehouseId,Number(input.initialQuantity),'PURCHASE',undefined,undefined,'İlk stok girişi');return product}
 private async defaultWarehouseId(){const c=this.companyId(),b=this.tenantContext.getBranchId();const r=await this.prisma.$queryRawUnsafe<any[]>(b?`SELECT id FROM inventory_warehouses WHERE company_id=$1::text AND branch_id=$2::text AND type='BRANCH' LIMIT 1`:`SELECT id FROM inventory_warehouses WHERE company_id=$1::text AND type='MAIN_DEPOT' LIMIT 1`,c,...(b?[b]:[]));if(!r.length)throw new BadRequestException('Inventory warehouse not found');return r[0].id}
 async addMovement(productId:string,warehouseId:string,quantity:number,type:string,unitCost?:number,referenceId?:string,note?:string){if(!Number.isFinite(quantity)||quantity<=0)throw new BadRequestException('Quantity must be greater than zero');const t=this.tenantId(),c=this.companyId();const product=await this.prisma.$queryRawUnsafe<any[]>(`SELECT id FROM inventory_products WHERE id=$1::text AND company_id=$2::text AND status='ACTIVE' LIMIT 1`,productId,c);if(!product.length)throw new NotFoundException('Product not found');const outbound=['ADJUSTMENT_OUT','DAMAGE','EXPIRED','SERVICE_CONSUMPTION','TRANSFER_OUT'].includes(type);if(outbound){const stock=await this.prisma.$queryRawUnsafe<any[]>(`SELECT quantity FROM inventory_stock WHERE product_id=$1::text AND warehouse_id=$2::text FOR UPDATE`,productId,warehouseId);if(!stock.length||Number(stock[0].quantity)<quantity)throw new BadRequestException('Insufficient stock')}await this.prisma.$executeRawUnsafe(`INSERT INTO inventory_stock(product_id,warehouse_id,quantity) VALUES($1::text,$2::text,0) ON CONFLICT DO NOTHING;UPDATE inventory_stock SET quantity=quantity+$3,updated_at=NOW() WHERE product_id=$1::text AND warehouse_id=$2::text;INSERT INTO inventory_movements(tenant_id,company_id,product_id,warehouse_id,type,quantity,unit_cost,reference_id,note) VALUES($4::text,$5::text,$1::text,$2::text,$6::"InventoryMovementType",$3,$7,$8::text,$9)`,productId,warehouseId,outbound?-quantity:quantity,t,c,type,unitCost??null,referenceId??null,note??null);return{success:true}}
 async movements(limit=80){const c=this.companyId();return this.prisma.$queryRawUnsafe<any[]>(`SELECT m.id,m.type,m.quantity,m.unit_cost AS "unitCost",m.note,m.created_at AS "createdAt",p.name AS "productName",p.unit,w.name AS "warehouseName" FROM inventory_movements m JOIN inventory_products p ON p.id=m.product_id JOIN inventory_warehouses w ON w.id=m.warehouse_id WHERE m.company_id=$1::text ORDER BY m.created_at DESC LIMIT $2`,c,limit)}
 async serviceMaterials(serviceId:string){return this.prisma.$queryRawUnsafe<any[]>(`SELECT ism.id,ism.product_id AS "productId",p.name AS "productName",p.unit,ism.quantity FROM inventory_service_materials ism JOIN inventory_products p ON p.id=ism.product_id JOIN services s ON s.id=ism.service_id WHERE ism.service_id=$1::text AND s.tenant_id=$2::text ORDER BY p.name`,serviceId,this.tenantId())}
 async setServiceMaterials(serviceId:string,materials:{productId:string;quantity:number}[]){const t=this.tenantId(),b=this.tenantContext.getBranchId();if(!b)throw new BadRequestException('A branch must be selected for this operation.');const s=await this.prisma.$queryRawUnsafe<any[]>(`SELECT id FROM services WHERE id=$1::text AND tenant_id=$2::text AND branch_id=$3::text LIMIT 1`,serviceId,t,b);if(!s.length)throw new NotFoundException('Service not found');await this.prisma.$executeRawUnsafe(`DELETE FROM inventory_service_materials WHERE service_id=$1::text`,serviceId);for(const m of materials){if(!Number.isFinite(m.quantity)||m.quantity<=0)throw new BadRequestException('Material quantity must be greater than zero');await this.prisma.$executeRawUnsafe(`INSERT INTO inventory_service_materials(service_id,product_id,quantity) VALUES($1::text,$2::text,$3)`,serviceId,m.productId,m.quantity)}return this.serviceMaterials(serviceId)}
 async purchaseRequests(){const c=this.companyId();return this.prisma.$queryRawUnsafe<any[]>(`SELECT pr.id,p.name AS "productName",w.name AS "warehouseName",pr.current_quantity AS "currentQuantity",pr.requested_quantity AS "requestedQuantity",pr.status,pr.reason,pr.created_at AS "createdAt" FROM inventory_purchase_requests pr JOIN inventory_products p ON p.id=pr.product_id JOIN inventory_warehouses w ON w.id=pr.warehouse_id WHERE pr.company_id=$1::text ORDER BY pr.created_at DESC`,c)}
 async suppliers(){return this.prisma.$queryRawUnsafe<any[]>(`SELECT id,name,contact_name AS "contactName",phone,email,tax_number AS "taxNumber",status FROM inventory_suppliers WHERE company_id=$1::text AND status='ACTIVE' ORDER BY name`,this.companyId())}
 async createSupplier(input:any){const t=this.tenantId(),c=this.companyId();if(!input.name?.trim())throw new BadRequestException('Supplier name is required');return (await this.prisma.$queryRawUnsafe<any[]>(`INSERT INTO inventory_suppliers(tenant_id,company_id,name,contact_name,phone,email,tax_number,address,notes) VALUES($1::text,$2::text,$3,$4,$5,$6,$7,$8,$9) RETURNING id,name,contact_name AS "contactName",phone,email,tax_number AS "taxNumber",status`,t,c,input.name.trim(),input.contactName||null,input.phone||null,input.email||null,input.taxNumber||null,input.address||null,input.notes||null))[0]}
 async transfers(){return this.prisma.$queryRawUnsafe<any[]>(`SELECT t.id,t.status,t.note,t.created_at AS "createdAt",s.name AS "sourceName",d.name AS "destinationName",COUNT(i.id)::int AS "itemCount" FROM inventory_transfers t JOIN inventory_warehouses s ON s.id=t.source_warehouse_id JOIN inventory_warehouses d ON d.id=t.destination_warehouse_id LEFT JOIN inventory_transfer_items i ON i.transfer_id=t.id WHERE t.company_id=$1::text GROUP BY t.id,s.name,d.name ORDER BY t.created_at DESC`,this.companyId())}
 async createTransfer(input:any){const t=this.tenantId(),c=this.companyId();if(!input.sourceWarehouseId||!input.destinationWarehouseId||input.sourceWarehouseId===input.destinationWarehouseId)throw new BadRequestException('Valid source and destination warehouses are required');if(!Array.isArray(input.items)||!input.items.length)throw new BadRequestException('Transfer must contain at least one item');const created=await this.prisma.$queryRawUnsafe<any[]>(`INSERT INTO inventory_transfers(tenant_id,company_id,source_warehouse_id,destination_warehouse_id,status,note) VALUES($1::text,$2::text,$3::text,$4::text,'PENDING',$5) RETURNING id`,t,c,input.sourceWarehouseId,input.destinationWarehouseId,input.note||null);for(const i of input.items){if(!Number.isFinite(Number(i.quantity))||Number(i.quantity)<=0)throw new BadRequestException('Invalid transfer quantity');await this.prisma.$executeRawUnsafe(`INSERT INTO inventory_transfer_items(transfer_id,product_id,quantity) VALUES($1::text,$2::text,$3)`,created[0].id,i.productId,Number(i.quantity))}return{...created[0],status:'PENDING'}}
}