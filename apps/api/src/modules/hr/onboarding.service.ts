import{BadRequestException,Injectable,NotFoundException}from'@nestjs/common';import{PrismaService}from'@beauty-erp/database';import{TenantContext}from'../../common/tenant/tenant-context';import{randomUUID}from'node:crypto';
const DEFAULT_TASKS=['Employment contract','Identity document','Bank details','SGK registration','KVKK documents','Uniform assignment','User account creation','Branch assignment','Device training','Service training','Occupational safety training','Mentor assignment','30-day evaluation'];
@Injectable()export class OnboardingService{constructor(private readonly prisma:PrismaService,private readonly ctx:TenantContext){}private scope(){const tenantId=this.ctx.getTenantId(),branchId=this.ctx.getBranchId(),roleScope=this.ctx.getRoleScope();if(!tenantId)throw new BadRequestException('Tenant context is required.');if(roleScope!=='CENTRAL'&&!branchId)throw new BadRequestException('A branch must be selected.');return{tenantId,branchId,roleScope}}private async staff(id:string){const{tenantId,branchId,roleScope}=this.scope();const s=await this.prisma.staff.findFirst({where:{id,tenantId,...(roleScope!=='CENTRAL'&&branchId?{branchId}:{})},select:{id:true,status:true,branchId:true,branch:{select:{companyId:true}}}});if(!s)throw new NotFoundException('Staff not found');return s}private date(v:any,name:string){if(v==null||v==='')return null;const d=new Date(v);if(Number.isNaN(d.getTime()))throw new BadRequestException(`Invalid ${name}.`);return d.toISOString().slice(0,10)}
async get(staffId:string){const{tenantId}=this.scope();await this.staff(staffId);const plans=await this.prisma.$queryRawUnsafe<any[]>(`SELECT * FROM hr_onboarding_plans WHERE tenant_id=$1 AND staff_id=$2 ORDER BY created_at DESC`,tenantId,staffId);if(!plans.length)return{plans:[],current:null,tasks:[],progress:0};const current=plans.find(x=>x.status==='ACTIVE')??plans[0],tasks=await this.prisma.$queryRawUnsafe<any[]>(`SELECT t.*,o."firstName" AS "ownerFirstName",o."lastName" AS "ownerLastName" FROM hr_onboarding_tasks t LEFT JOIN staff o ON o.id=t.owner_staff_id AND o."tenantId"=$1 WHERE t.tenant_id=$1 AND t.plan_id=$2 ORDER BY t.sort_order,t.created_at`,tenantId,current.id),done=tasks.filter(x=>x.status==='COMPLETED'||x.status==='SKIPPED').length;return{plans,current,tasks,progress:tasks.length?Math.round(done*100/tasks.length):0}}
async create(staffId:string,b:any,userId:string){const{tenantId}=this.scope(),s=await this.staff(staffId),id=randomUUID(),name=String(b.name??'Employee Onboarding').trim();if(!name)throw new BadRequestException('name is required.');if(s.status!=='ACTIVE')throw new BadRequestException('Onboarding can only be started for active staff.');const startedAt=this.date(b.startedAt,'startedAt'),target=this.date(b.targetCompletionDate,'targetCompletionDate');if(startedAt&&target&&target<startedAt)throw new BadRequestException('Target completion date cannot precede onboarding start date.');const raw=Array.isArray(b.tasks)&&b.tasks.length?b.tasks:DEFAULT_TASKS,titles=raw.map((x:any)=>String(typeof x==='string'?x:x?.title??'').trim()).filter(Boolean);if(!titles.length)throw new BadRequestException('At least one onboarding task is required.');if(new Set(titles.map((x:string)=>x.toLocaleLowerCase('en-US'))).size!==titles.length)throw new BadRequestException('Onboarding task titles must be unique.');return this.prisma.$transaction(async tx=>{await tx.$queryRawUnsafe<any[]>(`SELECT id FROM staff WHERE id=$1 AND "tenantId"=$2 FOR UPDATE`,staffId,tenantId);const active=await tx.$queryRawUnsafe<any[]>(`SELECT id FROM hr_onboarding_plans WHERE tenant_id=$1 AND staff_id=$2 AND status='ACTIVE' FOR UPDATE`,tenantId,staffId);if(active.length)throw new BadRequestException('An active onboarding plan already exists.');const rows=await tx.$queryRawUnsafe<any[]>(`INSERT INTO hr_onboarding_plans(id,tenant_id,company_id,branch_id,staff_id,name,status,started_at,target_completion_date,created_by) VALUES($1,$2,$3,$4,$5,$6,'ACTIVE',COALESCE($7::date,CURRENT_DATE),$8::date,$9) RETURNING *`,id,tenantId,s.branch.companyId,s.branchId,staffId,name,startedAt,target,userId);for(let i=0;i<titles.length;i++)await tx.$executeRawUnsafe(`INSERT INTO hr_onboarding_tasks(id,tenant_id,plan_id,title,sort_order) VALUES($1,$2,$3,$4,$5)`,randomUUID(),tenantId,id,titles[i],i);return rows[0]})}
async updateTask(staffId:string,taskId:string,b:any,userId:string){const{tenantId}=this.scope();await this.staff(staffId);const status=String(b.status??'').toUpperCase();if(!['PENDING','IN_PROGRESS','COMPLETED','SKIPPED','BLOCKED'].includes(status))throw new BadRequestException('Invalid onboarding task status.');if(status==='SKIPPED'&&!String(b.completionNote??'').trim())throw new BadRequestException('A completion note is required when skipping an onboarding task.');const dueDate=this.date(b.dueDate,'dueDate');if(b.ownerStaffId){const owner=await this.prisma.staff.findFirst({where:{id:b.ownerStaffId,tenantId,status:'ACTIVE'},select:{id:true}});if(!owner)throw new BadRequestException('Task owner must be an active staff member in the same tenant.')}return this.prisma.$transaction(async tx=>{const task=(await tx.$queryRawUnsafe<any[]>(`SELECT t.*,p.staff_id,p.status AS "planStatus" FROM hr_onboarding_tasks t JOIN hr_onboarding_plans p ON p.id=t.plan_id WHERE t.id=$1 AND t.tenant_id=$2 AND p.staff_id=$3 FOR UPDATE`,taskId,tenantId,staffId))[0];if(!task)throw new NotFoundException('Onboarding task not found');if(task.planStatus!=='ACTIVE')throw new BadRequestException('Onboarding plan is not active.');if(status==='COMPLETED'&&task.depends_on_task_id){const dep=(await tx.$queryRawUnsafe<any[]>(`SELECT status FROM hr_onboarding_tasks WHERE id=$1 AND tenant_id=$2 AND plan_id=$3 FOR UPDATE`,task.depends_on_task_id,tenantId,task.plan_id))[0];if(!dep)throw new BadRequestException('Dependent onboarding task is invalid.');if(!['COMPLETED','SKIPPED'].includes(dep.status))throw new BadRequestException('Dependent onboarding task must be completed first.')}await tx.$executeRawUnsafe(`UPDATE hr_onboarding_tasks SET status=$1,owner_staff_id=COALESCE($2,owner_staff_id),due_date=COALESCE($3::date,due_date),completion_note=COALESCE($4,completion_note),completed_at=CASE WHEN $1 IN ('COMPLETED','SKIPPED') THEN CURRENT_TIMESTAMP ELSE NULL END,completed_by=CASE WHEN $1 IN ('COMPLETED','SKIPPED') THEN $5 ELSE NULL END,updated_at=CURRENT_TIMESTAMP WHERE id=$6 AND tenant_id=$7`,status,b.ownerStaffId??null,dueDate,b.completionNote??null,userId,taskId,tenantId);const remaining=(await tx.$queryRawUnsafe<any[]>(`SELECT COUNT(*)::int AS count FROM hr_onboarding_tasks WHERE tenant_id=$1 AND plan_id=$2 AND status NOT IN ('COMPLETED','SKIPPED')`,tenantId,task.plan_id))[0]?.count??0;if(remaining===0)await tx.$executeRawUnsafe(`UPDATE hr_onboarding_plans SET status='COMPLETED',completed_at=CURRENT_TIMESTAMP,updated_at=CURRENT_TIMESTAMP WHERE id=$1 AND tenant_id=$2 AND status='ACTIVE'`,task.plan_id,tenantId);return{planId:task.plan_id,status}}).then(()=>this.get(staffId))} }
