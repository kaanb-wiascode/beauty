import{BadRequestException}from'@nestjs/common';import{ApprovalWorkflowService}from'./approval-workflow.service';
describe('ApprovalWorkflowService',()=>{const ctx={getTenantId:()=> 'tenant-1',getCompanyId:()=> 'company-1'} as any;it('rejects invalid approval actions before persistence',async()=>{const service=new ApprovalWorkflowService({} as any,ctx);await expect(service.act('instance-1','INVALID',{userId:'u1',roleId:'r1'})).rejects.toBeInstanceOf(BadRequestException)});it('requires identity fields on submit',async()=>{const service=new ApprovalWorkflowService({} as any,ctx);await expect(service.submit({entityType:'LEAVE'})).rejects.toBeInstanceOf(BadRequestException)});it('requires policy steps',async()=>{const service=new ApprovalWorkflowService({} as any,ctx);await expect(service.createPolicy({name:'Leave approval',entityType:'LEAVE',steps:[]})).rejects.toBeInstanceOf(BadRequestException)})});
